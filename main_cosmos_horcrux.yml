---
# Specifically for horcrux nodes - CAUTION
- name: Prepare and configure nodes
  hosts: horcrux
  gather_facts: false
  vars_files:
    - "{{ var_file_horcrux }}"
  roles:
    - util_install_go
    - cosmos_horcrux
