---
# Specifically for horcrux nodes - CAUTION
- name: Prepare and configure nodes
  hosts: horcrux
  gather_facts: false
  vars_files:
    - "{{ user_file }}"
    - "{{ var_file_horcrux }}"
  roles:
    # - { role: setup, become: true }
    # - { role: secure, become: true }
    # - cosmos_node_dependencies
    - cosmos_horcrux
