---
- name: Install and configure haproxy and varnish for Stargaze nodes
  hosts: all
  gather_facts: false
  roles:
    # - { role: util_install_varnish, when: ipfs_role is defined }
    - { role: util_install_haproxy, when: haproxy_config is defined }
