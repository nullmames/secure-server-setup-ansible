---
- name: Uninstall repo docker files is present
  become: true
  ansible.builtin.apt:
    name:
      - docker
      - docker-engine
      - docker.io
      - containerd
      - runc
    state: absent

- name: Install docker repository key
  become: true
  ansible.builtin.get_url:
    url: "https://download.docker.com/linux/debian/gpg"
    dest: /usr/share/keyrings/docker.asc
    mode: "0644"
    force: true

- name: Install docker repository
  become: true
  ansible.builtin.apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/docker.asc] https://download.docker.com/linux/debian {{ ansible_distribution_release }} stable"
    state: present
    update_cache: true

- name: Install docker components
  become: true
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - python3-pip
    state: present

- name: Remove Compose 2.0 component
  become: true
  ansible.builtin.apt:
    name:
      - docker-compose-plugin
    state: absent

- name: Add ansible user to docker group
  become: true
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    groups: docker
    append: true

- name: Install docker python package
  ansible.builtin.pip:
    name:
      - docker
      - docker-compose
