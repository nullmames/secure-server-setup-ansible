---
- name: Cosmos Full Send
  hosts: all
  gather_facts: true
  vars_files:
    - "{{ var_file }}"
    - "{{ user_file }}"

- name: Init
  ansible.builtin.import_playbook: cosmos_node_init.yml

# - name: restart
#   ansible.builtin.import_playbook: cosmos_node_restart_binary.yml
# - name: Peer node
#   ansible.builtin.import_playbook: cosmos_node_local_peers.yml

- name: Statesync node
  ansible.builtin.import_playbook: cosmos_node_reset_snapshot.yml
