---
- name: Install or reinstall ipfs and ipfs-cluster
  hosts: stargaze_api
  gather_facts: true
  roles:
    - { role: util_install_ipfs, when: ipfs_role is defined }
    - { role: util_install_ipfs_cluster, when: ipfs_role is defined }
    - util_install_docker
    - util_install_imgproxy
