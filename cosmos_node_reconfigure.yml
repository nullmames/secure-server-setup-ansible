---
- name: Cosmos reconfigure and restart binary
  hosts: all
  gather_facts: false
  vars_files:
    - "{{ var_file }}"
    - "{{ user_file }}"
  roles:
    - cosmos_node_configure
    - cosmos_node_restart_binary
